package com.nemonotfound.nemos.inventory.sorting.config.model;

public record ComponentConfig(String componentName, boolean isEnabled, int xOffset, Integer yOffset, int width,
                              int height) {
}
