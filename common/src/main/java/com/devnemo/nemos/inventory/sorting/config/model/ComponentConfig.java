package com.devnemo.nemos.inventory.sorting.config.model;

public record ComponentConfig(String componentName, boolean isEnabled, Integer xOffset, Integer yOffset, int width,
                              int height) {
}
